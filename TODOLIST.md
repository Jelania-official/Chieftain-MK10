# 坦克控制板原理图与电源设计待办清单

## 目标

这份清单用于整理当前控制板原理图还需要补充、确认和修正的部分，重点覆盖：

- 电源去耦与储能电容
- 电机/驱动/传感器供电稳定性
- 电池电压检测
- 调试与上电安全
- 当前原理图中需要复核的连接

这份清单默认基于当前系统结构：

- 主电源：`3S 锂电池 / 12V 母线`
- 降压：`12V -> 5V`、`12V -> 3.3V`
- 主控：`ESP32 开发板`
- 行走驱动：`TB6612FNG`
- 炮塔方位：`SimpleFOC Mini + 无刷电机 + AS5600`
- 姿态传感器：`MPU6050`
- 俯仰执行器：`5V 舵机`

---

## 一、必须补的去耦/储能电容

原则：

- 所有电容都并联在 `电源` 和 `GND` 之间
- `0.1uF` 负责高频噪声
- 大电解负责电流冲击和压降缓冲
- 电容应尽量靠近对应模块的供电脚或供电入口

---

### 1. 12V 主电源入口

位置：

- 电池/12V 进入整块板后的主电源入口处

器件：

- `C1 = 1000uF / 25V` 电解
- `C2 = 0.1uF / 50V X7R / 0805`

连接：

- `12V -> C1正极`
- `C1负极 -> GND`
- `12V -> C2一端`
- `C2另一端 -> GND`

作用：

- 抵抗整板瞬时大电流
- 吸收电机和驱动带来的母线波动

---

### 2. TB6612FNG 电机电源入口

位置：

- `U4` 的 `12V` 与 `GND` 附近

器件：

- `C3 = 470uF / 16V` 电解
- `C4 = 0.1uF / 50V X7R / 0805`

连接：

- `U4-12V -> C3正极`
- `C3负极 -> GND`
- `U4-12V -> C4一端`
- `C4另一端 -> GND`

作用：

- 缓冲行走电机启停带来的电压跌落
- 抑制驱动开关噪声

---

### 3. SimpleFOC Mini 的 12V 输入

位置：

- `FOC1` 的 `12V` 和 `GND` 附近

器件：

- `C5 = 470uF / 16V` 电解
- `C6 = 0.1uF / 50V X7R / 0805`

连接：

- `FOC1-12V -> C5正极`
- `C5负极 -> GND`
- `FOC1-12V -> C6一端`
- `C6另一端 -> GND`

作用：

- 缓冲无刷驱动的电流脉动
- 降低方位电机工作时对整板的干扰

---

### 4. 5V 总线入口

位置：

- `12V_TO_5V` 模块输出接入整板 `5V` 母线的位置

器件：

- `C7 = 470uF / 10V` 电解
- `C8 = 0.1uF / 50V X7R / 0805`

连接：

- `5V -> C7正极`
- `C7负极 -> GND`
- `5V -> C8一端`
- `C8另一端 -> GND`

作用：

- 稳定整条 5V 电源
- 给舵机和 ESP32 供电提供缓冲

---

### 5. ESP32 的 VIN 入口

位置：

- `U1 VIN` 与 `GND` 附近

器件：

- `C9 = 47uF / 10V` 电解或贴片铝电解
- `C10 = 1uF / 25V X7R / 0805`
- `C11 = 0.1uF / 50V X7R / 0805`

连接：

- `VIN -> C9正极`
- `C9负极 -> GND`
- `VIN -> C10一端`
- `C10另一端 -> GND`
- `VIN -> C11一端`
- `C11另一端 -> GND`

作用：

- 缓冲 ESP32 输入电源
- 降低上电和蓝牙通信时的供电波动

---

### 6. 3.3V 总线入口

位置：

- `12V_TO_3.3V` 输出接入整板 `3.3V` 母线的位置

器件：

- `C12 = 22uF / 10V X7R`
- `C13 = 0.1uF / 50V X7R / 0805`

连接：

- `3.3V -> C12一端`
- `C12另一端 -> GND`
- `3.3V -> C13一端`
- `C13另一端 -> GND`

作用：

- 稳定 3.3V 逻辑和传感器电源

---

### 7. MPU6050 供电去耦

位置：

- `MPU6050` 的 `VCC` 和 `GND` 附近

器件：

- `C14 = 0.1uF / 50V X7R / 0805`
- `C15 = 1uF / 25V X7R / 0805`

连接：

- `3.3V -> C14 -> GND`
- `3.3V -> C15 -> GND`

作用：

- 减少 I2C 和姿态测量噪声

---

### 8. AS5600 / 无刷编码器接口去耦

位置：

- `P1` 的 `VCC` 和 `GND` 附近

器件：

- `C16 = 0.1uF / 50V X7R / 0805`
- `C17 = 1uF / 25V X7R / 0805`

连接：

- `3.3V -> C16 -> GND`
- `3.3V -> C17 -> GND`

作用：

- 稳定编码器读数

---

### 9. 左右行走编码器电源去耦

位置：

- 左右编码器接口的 `3.3V` 与 `GND` 附近

器件：

- 左侧：
  - `C18 = 0.1uF / 50V X7R / 0805`
  - `C19 = 1uF / 25V X7R / 0805`
- 右侧：
  - `C20 = 0.1uF / 50V X7R / 0805`
  - `C21 = 1uF / 25V X7R / 0805`

连接：

- `3.3V -> 电容 -> GND`

作用：

- 稳定开漏编码器供电
- 降低行走电机噪声串入编码器信号

---

### 10. 舵机 5V 支路

位置：

- 炮塔接口 / 电滑环输出到舵机 `5V` 的支路附近

器件：

- `C22 = 1000uF / 10V` 电解
- `C23 = 0.1uF / 50V X7R / 0805`

连接：

- `5V -> C22正极`
- `C22负极 -> GND`
- `5V -> C23一端`
- `C23另一端 -> GND`

作用：

- 舵机动作时防止 5V 总线瞬间下跌

---

### 11. 电池电压检测 ADC 滤波

位置：

- `VN` 分压点到地之间

器件：

- `C24 = 0.1uF / 50V X7R / 0805`

连接：

- `VN -> C24一端`
- `C24另一端 -> GND`

作用：

- 让 ADC 采样更稳定
- 降低 PWM 和电机噪声对电压检测的影响

---

## 二、建议固定使用的阻容数值

### 电阻

- `I2C 上拉 = 4.7k / 0805`
- `编码器开漏上拉 = 10k / 0805`
- `逻辑默认上拉/下拉 = 10k / 0805`
- `电池分压上臂 R6 = 100k / 0805`
- `电池分压下臂 R7 = 33k / 0805`

### 小电容

- `0.1uF / 50V X7R / 0805`
- `1uF / 25V X7R / 0805`
- `22uF / 10V X7R`

### 大电容

- `47uF / 10V` 电解或贴片铝电解
- `470uF / 10V` 或 `470uF / 16V` 电解
- `1000uF / 10V` 电解
- `1000uF / 25V` 电解

---

## 三、原理图还需要完善和复核的地方

1. TB6612 的 nSTBY 不能悬空

当前待确认：

- `nSTBY` 是否已经接到 ESP32
- 是否已经有默认上拉

建议：

- `nSTBY` 接一个 ESP32 GPIO
- 同时加 `10k` 上拉到 `3.3V`

如果暂时不做控制：

- 至少先直接拉到 `3.3V`

否则现象可能是：

- 驱动处于待机
- 电机完全不转

---

2. TB6612 输出接法必须再确认

需要确认：

- 左电机是否接在 `AO1` 和 `AO2` 之间
- 右电机是否接在 `BO1` 和 `BO2` 之间

正确方式：

- `AO1 <-> 左电机 <-> AO2`
- `BO1 <-> 右电机 <-> BO2`

不能这样接：

- 一个驱动输出到电机
- 电机另一端直接接地

---

3. MPU6050 的 I2C 连接要确认没混到 GND

需要确认网络：

- `VCC -> 3.3V`
- `GND -> GND`
- `SCL -> SCL_C`
- `SDA -> SDA_C`

必须确认：

- `SCL_C` 和 `SDA_C` 没有与 GND 混网

---

4. AS5600 那条 I2C 总线也要确认上拉

当前要确认：

- `SDA_FOC`
- `SCL_FOC`

如果模块本体没有上拉，则补：

- `4.7k` 上拉到 `3.3V`

---

### 5. ESP32 外部 5V 供电建议加防回灌

目标：

- 在 PCB 正常供电时，仍可插 Type-C 打日志

建议增加：

- `D1 = SS14` 肖特基二极管

连接：

- `5V母线 -> D1 -> U1 VIN`

作用：

- 降低外部 5V 和 USB 5V 互相顶电的风险

---

### 6. ESP32 的调试串口建议预留

建议在 PCB 上预留一个调试接口：

- `GND`
- `TX_DEBUG`
- `RX_DEBUG`（可选）
- `3.3V`（可选）

如果后面只看日志：

- `GND + TX` 两根就够

---

### 7. 电池分压检测网络已基本正确，但要补滤波

当前逻辑：

- `R6 = 100k`
- `R7 = 33k`
- 接到 `VN`

需要补：

- `C24 = 0.1uF` 到地

并继续保持：

- 电池负极与系统 GND 共地

---

### 8. 参考标号不要重名

当前截图中存在：

- 多个 `U2`

建议整理：

- 电源模块、MPU6050、其他模块的参考标号唯一化

否则后续会影响：

- PCB 对照
- BOM
- 排错

---

### 9. 电源与地线布局要区分“脏电”和“净电”

建议：

- `TB6612`
- `SimpleFOC`
- `舵机 5V`

这几类大电流支路尽量就近回地，再汇入主地

而：

- `ESP32`
- `MPU6050`
- `AS5600`
- `编码器`

这几类敏感器件尽量走较干净的 3.3V 和 GND

---

## 四、嘉立创原理图绘制时的直接待办

### 电源部分

- [ ] 在 `12V` 主入口补 `1000uF + 0.1uF`
- [ ] 在 `12V_TO_5V` 输出后的 `5V` 母线补 `470uF + 0.1uF`
- [ ] 在 `12V_TO_3.3V` 输出后的 `3.3V` 母线补 `22uF + 0.1uF`
- [ ] 在 `ESP32 VIN` 旁补 `47uF + 1uF + 0.1uF`
- [ ] 在 `TB6612 12V` 旁补 `470uF + 0.1uF`
- [ ] 在 `FOC1 12V` 旁补 `470uF + 0.1uF`
- [ ] 在 `舵机 5V` 支路补 `1000uF + 0.1uF`

### 传感器部分

- [ ] 在 `MPU6050` 旁补 `0.1uF + 1uF`
- [ ] 在 `AS5600` 旁补 `0.1uF + 1uF`
- [ ] 在左编码器电源旁补 `0.1uF + 1uF`
- [ ] 在右编码器电源旁补 `0.1uF + 1uF`
- [ ] 在 `VN` 采样点补 `0.1uF` 滤波

### 逻辑与接口部分

- [ ] 确认 `SDA_C / SCL_C` 已正确上拉 `4.7k`
- [ ] 确认 `SDA_FOC / SCL_FOC` 已正确上拉 `4.7k`
- [ ] 确认 `R_A / R_B / L_A / L_B` 已正确上拉 `10k`
- [ ] 给 `TB6612 nSTBY` 加 `10k` 上拉，或接到 ESP32 控制
- [ ] 预留调试串口接口
- [ ] 考虑在 `5V -> VIN` 路径加入 `SS14`

### 连线复核

- [ ] 核对 TB6612 输出是否为 `AO1-AO2` / `BO1-BO2` 成对接电机
- [ ] 核对 MPU6050 的 `SDA/SCL` 未误接地
- [ ] 核对所有模块参考标号不重名
- [ ] 核对 `VN` 分压网络和系统地共地

---

## 五、后续建议

原理图补完后，建议下一步按顺序做：

1. 先完成 ERC 检查，消掉重名和未连接警告
2. 再画 PCB，先摆电源模块、大电流器件和大电容
3. 电机、舵机、FOC 的电源线和地线优先加粗
4. `VN`、I2C、编码器信号尽量远离大电流 PWM 区域
5. 完成 PCB 后，再进行一次电源完整性复核

这样后面第一次上电成功率会高很多。
