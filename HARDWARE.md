# Chieftain MK10 1:32 Hardware Notes

本项目用于实现 1:32 比例坦克模型的静改动，目标是尽量模拟真实坦克的底盘惯性、坡度响应、炮塔/炮管双轴稳定和手柄操控行为。

## 1. 主控与任务分配

主控: ESP32-WROOM-32 DevKit V1, 30-Pin

特性: 双核 32 位处理器, 240 MHz, 内置 Wi-Fi/蓝牙

调试接口: USB Type-C, CH9102 串口芯片

软件框架: PlatformIO + Arduino framework。ESP32 Arduino Core 已内置 FreeRTOS, 当前项目不需要额外添加 FreeRTOS 源码。

任务分配:

- Core 0: `FOC_Task`, 负责 SimpleFOC 高频环路 `loopFOC()` / `move()`。
- Core 1: Arduino 默认 `setup()` / `loop()`, 负责 Xbox 蓝牙、双 MPU6050 读取、底盘动力学、PID 稳定、舵机和行走电机逻辑。

## 2. 当前引脚分配

### I2C 总线

- AS5600/FOC 总线: `SDA=16`, `SCL=17`, 使用 `Wire`, 400 kHz。
- 双 MPU6050 总线: `SDA=21`, `SCL=22`, 使用 `Wire1`, 400 kHz。

### 底盘 TB6612FNG

- 右侧电机: `IN1=25`, `IN2=33`, `PWM=32`。
- 左侧电机: `IN1=26`, `IN2=27`, `PWM=14`。
- PWM 频率: 10 kHz。
- PWM 分辨率: 8 bit。

### 行走电机编码器

- 右侧编码器: `A=23`, `B=4`, `PCNT_UNIT_0`。
- 左侧编码器: `A=35`, `B=34`, `PCNT_UNIT_1`。
- 注意: GPIO 34/35 仅输入, 需要外部上拉电阻。

### 炮塔与炮管

- 炮管俯仰舵机: `GPIO15`, 333 Hz, 脉宽范围 500-2500 us。
- 炮塔无刷电机 PWM: `A=5`, `B=19`, `C=18`。
- 炮塔角度反馈: AS5600, I2C 默认地址 `0x36`。

## 3. 炮塔方位角控制系统

执行器: 2804 云台无刷电机, 中空轴版本。

槽极结构: 12N14P, 极对数 `Pole Pairs = 7`。

已知参数: KV220, 相电阻 2.55 ohm。

驱动器: SimpleFOC Mini v1.0, 基于 MS8313。

控制模式: 3 路 PWM 输入, SimpleFOC 扭矩模式。

反馈传感器: AS5600 磁编码器, 12 bit。

## 4. 炮管俯仰控制系统

执行器: 7350 MG-D 数字舵机。

电机类型: 空心杯, 金属齿轮。

信号: 标准舵机 PWM, 当前代码使用 333 Hz。

待确认参数:

- 推荐工作电压。4.8-6V
- 堵转电流和持续工作电流。6V时500ma、130ma
- 扭矩规格。0.08s/60°  1.5kg/CM
- 机械行程范围。180°+-10°

## 5. 底盘行走系统

驱动器: TB6612FNG 双通道直流电机驱动板。

执行器: N20 减速电机, 带 AB 相磁性霍尔编码器。

代码中的物理参数:

- 编码器基础脉冲: `7 PPR`。
- 减速箱变比: `59:1`。
- 主动轮直径: `17 mm`。
- 模型比例: `1:32`。
- 真车等效前进极速: `48 km/h`。
- 真车等效倒车极速: `11 km/h`。

待确认参数:

- N20 电机额定电压。12v
- 空载转速。540rpm
- 堵转电流。800ma
- 编码器供电电压。3.3/5V
- 左右电机实际安装方向是否与代码方向一致。还没安装，待确认

## 6. 姿态感知层

传感器: GY-521 MPU6050 x2, 6 轴 IMU, 包含三轴陀螺仪和三轴加速度计。

部署:

- 车体 IMU: 固定于底盘, 用于检测车体俯仰、坡度和颠簸。
- 炮管/炮塔 IMU: 安装于炮管或随炮管运动结构, 用于反馈实际指向。

地址分配:

- 车体 IMU: `0x68`, AD0 接地。
- 炮管/炮塔 IMU: `0x69`, AD0 接高电平。

说明:

- 加速度计在静止或低动态时可以测到重力方向, 可用于估算 pitch/roll。
- 陀螺仪测的是角速度, 不能直接测重力方向；当前代码用陀螺仪积分 + 加速度计重力方向做互补滤波。
- MPU6050 不能靠加速度计得到可靠 yaw 绝对角度, yaw 只能靠陀螺仪积分或额外磁力计/编码器修正。

待确认参数:

- 两个 IMU 的安装方向和轴定义。还没安装，待确认
- 车头方向对应 MPU6050 哪个轴。
- 炮管抬头/低头对应 MPU6050 哪个轴。

## 7. 电源管理

主供电: 建议 3S 锂电池, 11.1-12.6 V。

降压模块: 4 A DC-DC, 500 kHz。

供电支路:

- 12 V/电池直供: SimpleFOC Mini 电源输入, TB6612FNG `VM`。
- 5 V: 舵机电源, TB6612FNG 逻辑电源。
- 3.3 V: ESP32, MPU6050, AS5600, 编码器逻辑电源。

电源注意事项:

- 电机/舵机电源建议与传感器/主控电源分支供电, 降低噪声串扰。
- 控制信号必须共地, 否则 PWM/I2C/编码器信号没有共同参考电位。
- 舵机和电机电源支路建议加足够电解电容, 防止大电流瞬变导致 ESP32 复位。

待确认参数:

- 电池容量与 C 数。未确认
- DC-DC 模块输入范围、输出电压和持续电流。6.5-16v  3.3v和5v  最大4A
- TB6612FNG 单通道持续电流/峰值电流。2A/3.2A
- SimpleFOC Mini 供电范围和最大相电流设置。8-30V  相电流设计没懂
